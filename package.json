{
  "name": "node-sasl-wrapper",
  "version": "0.0.0",
  "description": "A minimal wrapper around the Cyrus SASL library",
  "main": "index.js",
  "keywords": [
    "sasl"
  ],
  "scripts": {
    "configure": "node-gyp configure --debug",
    "build": "node-gyp build --debug --verbose",
    "test": "mocha"
  },
  "devDependencies": {
    "mocha": "^7.1.1",
    "node-gyp": "^6.1.0"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "nan": "^2.14.0"
  }
}
